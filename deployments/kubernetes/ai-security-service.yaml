apiVersion: v1
kind: Service
metadata:
  name: ai-security
  namespace: payment-gateway
spec:
  selector:
    app: ai-security
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
  type: ClusterIP
